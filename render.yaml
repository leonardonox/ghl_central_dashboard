services:
  - type: web
    name: ghl-central-dashboard
    env: python
    rootDir: ghl_central_dashboard
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
